CREATE TABLE IF NOT EXISTS EXTERNAL_SERVICES (
    ID CHAR(36) PRIMARY KEY,
    NAME VARCHAR(255) NOT NULL,
    URL TEXT NOT NULL,
    PROMPT TEXT,
    TIMEOUT_MS INTEGER DEFAULT 5000,
    RETRY_COUNT INTEGER DEFAULT 3,
    IS_LLM BOOLEAN DEFAULT FALSE,
    CREATED_AT TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    INDEX IDX_EXTERNAL_SERVICES_NAME (NAME(100))
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

CREATE TABLE IF NOT EXISTS EXTERNAL_SERVICE_HEADERS (
    ID INT AUTO_INCREMENT PRIMARY KEY,
    SERVICE_ID CHAR(36) NOT NULL,
    HEADER_KEY VARCHAR(100) NOT NULL,
    HEADER_VALUE TEXT NOT NULL,
    CATEGORY ENUM('STANDARD', 'SECURITY') DEFAULT 'STANDARD',
    INDEX IDX_EXTERNAL_SERVICE_HEADERS_SERVICE_ID (SERVICE_ID),
    CONSTRAINT fk_service
        FOREIGN KEY (SERVICE_ID) 
        REFERENCES EXTERNAL_SERVICES(ID) 
        ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
